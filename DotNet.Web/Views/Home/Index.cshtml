@{
    //List<PermissMenu> firstLevelMenus = ViewBag.FirstLevelMenus as List<PermissMenu>;
    //List<PermissMenu> secondLevelMenus = ViewBag.SecondLevelMenus as List<PermissMenu>;
    //PermissUserLogin user = ViewBag.UserSession;
    ViewData["Title"] = "管理中心";
    ViewBag.BodyClass = "hold-transition skin-blue sidebar-mini";
}
@section Head
    {
    <!-- Font Awesome -->
    <link href="~/css/font-awesome.min.css" rel="stylesheet" />
    <!-- Ionicons -->
    <link href="~/bower_components/Ionicons/css/ionicons.min.css" rel="stylesheet" />
    <!-- Theme style -->
    <link rel="stylesheet" href="~/lib/AdminLTE-2.4.0-rc/dist/css/AdminLTE.min.css">
    <link rel="stylesheet" href="~/lib/AdminLTE-2.4.0-rc/dist/css/skins/_all-skins.min.css">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="~/js/html5shiv.min.js"></script>
        <script src="~/js/respond.min.js"></script>
    <![endif]-->
}

<div class="wrapper">
    @{ Html.RenderPartial("_HeadBar"); }
    <!-- 左侧导航栏 -->
    @{ Html.RenderPartial("_LeftSideBar"); }
    <!-- 中间区域 -->
    @{ Html.RenderPartial("_Content"); }
    <!-- Footer -->
    @{ Html.RenderPartial("_FooterBar"); }
    <!-- 右侧控制栏 -->
    @{ Html.RenderPartial("_RightSideBar"); }
</div>

@section Scripts
    {

    <script>
        require.config({
            baseUrl: '/js/pages'
        });
        require(['home'], function (home) {
            home.Init();
        });
    </script>
    <script src="~/plugins/ControlSidebar.js"></script>
    <script src="~/plugins/PushMenu.js"></script>
    <!-- AdminLTE App -->
    <script src="~/lib/AdminLTE-2.4.0-rc/dist/js/app.min.js"></script>
    <!-- Sparkline -->
    @*<script src="~/bower_components/jquery-sparkline/dist/jquery.sparkline.min.js"></script>*@
    <!-- SlimScroll 1.3.0 -->
    <script src="~/bower_components/jquery-slimscroll/jquery.slimscroll.min.js"></script>
    <!-- AdminLTE for demo purposes -->
    <script src="~/lib/AdminLTE-2.4.0-rc/dist/js/demo.js"></script>

    <script>
        //function createIframe(url, title) {
        //    $("#titleSpan").text(title);
        //    $('#contentFrame').attr('src', '../' + url);
        //}

        //function reinitIframe() {
        //    var iframe = document.getElementById("contentFrame");
        //    try {
        //        var bHeight = iframe.contentWindow.document.body.scrollHeight;
        //        var dHeight = iframe.contentWindow.document.documentElement.scrollHeight;
        //        var height = Math.max(bHeight, dHeight);
        //        iframe.height = height;
        //    } catch (ex) {
        //    }
        //}

        //var timer1 = window.setInterval("reinitIframe()", 500);
        ////定时开始

        //function reinitIframeEND() {
        //    var iframe = document.getElementById("contentFrame");
        //    try {
        //        var bHeight = iframe.contentWindow.document.body.scrollHeight;
        //        var dHeight = iframe.contentWindow.document.documentElement.scrollHeight;
        //        var height = Math.max(bHeight, dHeight);
        //        iframe.height = height;
        //    } catch (ex) {
        //    }
        //    // 停止定时
        //    window.clearInterval(timer1);
        //}

        //function SignOut() {
        //    if (confirm("确定要退出登录？")) {
        //        $.post("/AdminHome/SignOut", {}, function (data) {
        //            if (data == 1) {
        //                location.reload(true);
        //            }
        //        });
        //    }
        //}
    </script>

}

